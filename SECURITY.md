# Security policy

The filter treats raster headers and pixel streams as untrusted input. The
latest release is the only supported version for security fixes.

Please report suspected vulnerabilities privately through the repository's
GitHub **Security** tab instead of opening a public issue. Include a minimal
raster reproducer where possible, but remove document content, printer serial
numbers, device URIs, and other private data.
